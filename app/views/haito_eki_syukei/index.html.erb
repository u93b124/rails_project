<h1>配当金集計</h1>
<h3>（受渡日：<font color = "red"><%= @nen %></font>年01月01日～
<font color = "red"><%= @nen %></font>年12月31日）</h3><br>

<% sum_plus = 0 %>
<% if @haito_eki_syukeis != nil %>
  <% @haito_eki_syukeis.each do |haito_eki| %>
    <% sum_plus = sum_plus + haito_eki.son_eki_gaku %>
  <% end %>
<% end %>

<table border="1">
    <tr>
      <th>配当額</th>
      <th>課税金額</th>
      <th>配当利益</th>
    </tr>
    <tr>
      <td><%= number_with_delimiter(sum_plus) %></td>
      <td><%= number_with_delimiter((sum_plus*Constants::TAX).floor) %></td>
      <td>
        <font color = "red">
          <%= number_with_delimiter((sum_plus*(1 - Constants::TAX)).floor) %>
        </font>
      </td>
    </tr>
</table>
<br>

<% (2020..Date.current.year).each do |year| %>
  <%= link_to "#{year}年", haito_eki_syukei_path(nen: year.to_s) %>
<% end %>


<table border="1">
  <thead>
    <tr>
      <th>順位</th>
      <th>コード</th>
      <th>会社名</th>
      <th>株数</th>
      <th>配当額</th>
      <th>受渡日</th>
    </tr>
    </thead>

<% if @haito_eki_syukeis != nil %>
  <div id="son_ekis">
    <% i = 0 %>
    <% @haito_eki_syukeis.each do |haito_eki| %>
      <tr>
        <% i = i + 1 %>
        <td><%= i %></td>
        <td><%= haito_eki.code %></td>
        <td><%= haito_eki.name %></td>
        <td align="right"><%= number_with_delimiter(haito_eki.kabu_su) %></td>
        <td align="right">
          <%= number_with_delimiter(haito_eki.son_eki_gaku) %>
        </td>
        <td><%= haito_eki.ukewatasi_bi %></td>
      </tr>
    <% end %>
  </div>
<% end %>


</table>

