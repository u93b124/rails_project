<%= form_with(model: diary) do |form| %>
  <% if diary.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(diary.errors.count, "error") %> prohibited this diary from being saved:</h2>

      <ul>
        <% diary.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title,"タイトル", style: "display: block" %>
    <%= form.text_field :title %>
  </div>
<br>
  <div>
    <%= form.label :body,"本文", style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <% if @diary.id %>
      <%= submit_tag("更新する") %>
    <% else %>
      <%= submit_tag("登録する") %>
    <% end %>  
  </div>
<% end %>
